{
  "name": "BeyBuilderX",
  "private": true,
  "version": "1.4.0",
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "eslint": "^9.35.0",
    "globals": "^17.12.0",
    "http-server": "^14.1.1"
  },
  "scripts": {
    "start": "cd ../ && npx http-server -p 8080 -c-1 --base-dir /BeyBuilderX/",
    "test": "node unit_tests/migration_001_stadium.test.js",
    "lint": "eslint --config .config/eslint.config.js ."
  },
  "scriptsComments": {
    "start": "Runs local server for PWA testing, using the same /BeyBuilderX/ path as Github hosting",
    "test": "Runs Node unit tests under unit_tests/"
  }
}
